- hosts: 127.0.0.1
  connection: local
  #become: true

  tasks:
    - name: Install Python
      apt: pkg = {{ item }} state = lastest
      with_items:
        - libcupti-dev
        - python3-numpy
        - python3-dev 
        - python3-wheel
        
    - name: install Required Packages
      apt: pkg = {{ item }} state = lastest
      with_items:
          - realmd
          - sssd
          - sssd-tools
          - libnss-sss
          - libpam-sss
          - adcli
          - oddjob
          - oddjob-mkhomedir
          - samba-common
  
